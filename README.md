# install 
```bash


sudo apt-get update
sudo apt-get install python3-full python3-dev python3-setuptools python3-pip python3-wheel libjpeg-dev zlib1g-dev tmux git firefox firefox-locale-de 


python3 -m pip install selenium flask webdriver-manager pillow jwt pytz --break-system-packages


git clone https://github.com/Max-42/flisticker.git
```

